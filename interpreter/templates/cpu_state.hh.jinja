// Generated by lira-simgen-lib (simple-interpreter). Do not edit.

#ifndef GENERATED_RISCV_CPUSTATE_HH_INCLUDED
#define GENERATED_RISCV_CPUSTATE_HH_INCLUDED

#include "isa.hh"

#include <array>
#include <concepts>
#include <cstddef>
#include <cstdint>
#include <fmt/core.h>
#include <fmt/ostream.h>

namespace prot::state {

class CPU final {
public:
{{ rf_code }}
  // Instruction count
  std::size_t m_icount{0};

  // Finished flag
  bool m_finished{false};

  // Exit code
  isa::Word m_code{0};

  // Interface functions
{%- for decl in decls %}
  {{ decl }}
{%- endfor %}
  // Function to stop the CPU execution
  void doExit(isa::Word code) {
    m_finished = true;
    m_code = code;
    fmt::println("Exiting with code {}...", code);
  }

  // Function to increase instruction count
  void increaseICount() {
    ++m_icount;
  }
};

} // prot::state

#endif // GENERATED_RISCV_CPUSTATE_HH_INCLUDED
